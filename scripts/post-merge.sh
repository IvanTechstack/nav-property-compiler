#!/bin/bash
set -e

# Post-merge setup for Ivan's Image Optimizer (Streamlit / Python project)
# Python deps are pre-installed in .pythonlibs — no install step needed.
# The Streamlit workflow is reconciled automatically after merge.

echo "Post-merge setup complete (no-op for Python/Streamlit project)."
