Let's completely re-architect the user interface layout and storage structure in main.py to transition this into a visual, Finder-style asset manager. Please implement this comprehensive structure in one unified update:

1. Visual Finder Layout: In the 'Browse bucket' section, completely remove dropdown menus and text-filtering boxes as the primary view. Instead, render a visual grid of Folder Icons representing each property path, sorted alphabetically. Clicking a folder icon must visually 'open' that directory to display the images inside, with a clear '⬅️ Back to Folders' navigation button at the top.
2. Master Featured Storage: Re-wire the upload pipeline. Any asset processed through the 'Featured Banner' column must be uploaded directly to a single, permanent root folder named '000_MASTER_FEATURED_IMAGES/' using a standardized naming format (e.g., 'property-name-featured.webp'). 
3. Safe Property Folder Wipes: Because featured banners are safely isolated in the master folder, the property-specific gallery folders will only hold portfolio shots. Provide a prominent '🗑️ Delete Entire Folder' button inside the opened folder view (and a delete control on the Finder folder cards) that completely removes the gallery folder from R2, knowing the master featured image is safely archived.
4. Visual Grid Checkbox Deletions: Inside an opened folder view, remove all text-based multiselect dropdowns. Every image card must display a small native checkbox in its corner. Users can visually select multiple photos and click a single 'Delete Selected Images' button to execute a bulk cherry-picked deletion.
5. Gallery Sort & Reverse: Keep the quick-toggle 'Reverse Order' button and alphabetical/date sort bar at the top of the opened folder view to instantly adjust photographer sequences.

Please refactor main.py to implement this visual file-manager ecosystem, test for robust navigation routing, and automatically push the clean architecture update straight to my IvanTechstack GitHub repository.