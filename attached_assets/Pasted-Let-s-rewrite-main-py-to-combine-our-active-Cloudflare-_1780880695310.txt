Let's rewrite main.py to combine our active Cloudflare R2 image optimization pipeline into a unified 'Property Content Compiler' engine. Please implement this pure, native Streamlit layout and logic in one clean sweep:

1. Retain Active Upload Core: Keep the current text input for property address and the three optimization upload columns (Portfolio Gallery to 800px high WebP, Featured Banner to 1920px wide WebP, Story Cover for GIF/WebP) with their respective compression logic completely intact.
2. Immediate Sequence Reorder: Below the media package processing button, render a native list of the newly uploaded gallery images. Provide a single click '↕️ Reverse Sequence Order' button to immediately invert photographer reverse-shoots, alongside a simple native numeric input box beneath each thumbnail so users can visually adjust the gallery flow before compilation.
3. Embedded AI Content Generator: Add a large text area labeled 'Paste Zillow / MLS Raw Text Dump Here'. Also add a text input field for the 'Studeo.ai Interactive Booklet URL'. When the user clicks a 'Compile Listing' button, use an integrated OpenAI API handshake to parse that text dump. The backend must automatically isolate the listing statistics (MLS, Beds, Baths, SqFt, Year, Price, City) and expand the text details into 4 rich marketing segments: Full Description, Neighborhood Profile, Location/Commuter Access, and City Profile.
4. Output Responsive HTML Templates: Wrap the compiled R2 asset links and AI-parsed data fields into two native string templates:
   - Mode 'For Sale': Generates the complete high-end responsive HTML layout block featuring custom inline CSS branded with #990000 accents, vector SVG icons, the desktop floating stats card header, a mobile-adaptive headline layout card container (hiding desktop content and displaying a text stack on mobile screens), thumbnail gallery matrix, 24-bullet grid, CSS radio tab container hub, split-column Studeo.ai booklet link, and the Google Map iframe embed, complete with a single click '📋 Copy HTML Code' text output box.
   - Mode 'Sold': Purges the specific property portfolio gallery folder from R2 entirely to save storage costs (leaving the master featured folder untouched) and outputs an optimized 'How We Sold It' archive template using only the permanent featured hero link.
5. Marketing Copy Exporter: Output two additional clean text fields containing the 6 brief flyer bullet points and the ready-to-paste agent social media post copy.

Please refactor main.py to handle this native automation engine flow, ensure it runs cleanly on Streamlit Cloud without any iframe component hacks, and automatically push the updated code straight to my GitHub repository!