Please modify our `main.py` application file to ensure we have clear, absolute methods to permanently delete completed property listings out of our tracking systems once they are sold. Implement these two structural logic additions in one batch:

1. MOUNT A PERMANENT "DELETE ENTIRE PROJECT" BUTTON IN BROWSE BUCKET:
Inside `_render_folder_contents()`, inspect our opened folder top header controls columns matrix (`back_c, title_c, del_c, wipe_c`). Currently, `wipe_c` contains a button to clear folder images but leaves the project alive. Expand the columns matrix to include a new column wrapper block containing a permanent button labeled "❌ Delete Entire Project" (visible for all non-master property directories). When clicked, it must trigger an inline `st.warning` safety confirmation dialog asking: "Are you absolutely sure you want to permanently destroy this project? This will completely wipe all folder contents AND erase the data.json tracking tracking profile from storage." On confirmation, it must loop through and execute `delete_object(obj["Key"])` for every single key prefix matching that property ID—including the images, the sorting sheets, and the `properties/{prefix}/data.json` configuration file—effectively vanishing the property from our directory entirely.

2. ENFORCE AUTO-REMOVAL UPON DROPDOWN CONFIRMATION:
Inside `page_compile()`, look at the "Output Mode" logic loop routines when the selection radio toggle is set to "Sold". Currently, confirming Sold mode clears the gallery but leaves the structural tracking JSON configuration alive, meaning the sold address stays permanently stuck inside our "Load Existing Listing" dropdown menu menu list. Modify this workflow so that when a user completes the Sold compilation handshake pass, the system automatically purges the corresponding `properties/{prefix}/data.json` tracking tracking profile file off the storage engine array, completely clearing it from our master dropdown index list parameters instantly.

Verify that all session state variables update cleanly upon execution, ensure all layout grid panels remain responsive, and push the verified data lifecycle update to our repository branch.